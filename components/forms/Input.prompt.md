Text field used across forms — CNPJ lookup, login, document upload notes.

```jsx
<Input label="E-mail" placeholder="voce@empresa.com.br" />
<Input label="Senha" type="password" error="Senha incorreta" />
```
